---
title: "Unit 1. 파일럿 한 대 세팅"
description: "펌웨어·Thonny·배선·검증. 16대 작업 전 반드시 한 대로 인과 사슬을 익힙니다."
sidebar:
  order: 1
---


이번 시간에는 우리 교실에서 측정될 환경 데이터를 만들어 줄 **첫 번째 노드 한 대**를 직접 세팅해 보겠습니다. 16개 교실에 똑같은 박스를 풀어놓기 전에, 먼저 한 대로 ‘이 코드가 실제 보드 위에서 정말 동작하는지’를 손으로 확인해야 합니다. 16배의 시간을 16분의 1로 줄이는 가장 확실한 방법은 ‘한 번 제대로 만든 한 대’입니다.

이번 단원의 학습 흐름은 단순합니다. 라즈베리 파이 피코 2 WH에 마이크로파이썬(MicroPython) 펌웨어를 설치하고, 그 위에 우리가 미리 만들어 둔 세 개의 파이썬 파일을 올린 다음, 온습도 센서(SHT40)와 아날로그 조도 센서(Grove Light Sensor (P) v1.1)를 각각 알맞은 자리에 꽂아 보드를 부팅합니다. 모든 단계의 결과는 셸(Shell) 창에 한 줄씩 출력되므로, 무엇이 어디에서 어떻게 멈췄는지 즉시 알 수 있습니다.

여기서 한 가지 중요한 점을 미리 짚어 두겠습니다. 우리는 이번 단원에서 ‘코드를 새로 짜는 일’을 거의 하지 않습니다. 펌웨어 파일도 미리 만들어 두었고, 회로 배선도 표준이 정해져 있습니다. 그렇다면 이번 단원의 진짜 학습 포인트는 무엇일까요? 바로 ‘**무엇이 잘못되었을 때, 어디를 봐야 하는지를 익히는 일**’입니다. 한 대를 만들면서 보드의 LED, Thonny의 셸 메시지, 서버의 로그, 그리고 대시보드의 카드 — 이 네 가지 신호가 어떻게 묶여 있는지를 손으로 체험합니다.

## 이번 시간의 미션

작업이 끝났을 때 다음 네 가지가 모두 사실이어야 합니다.

1. 피코 본체의 LED가 켜져 있고, Thonny 셸에 30초마다 측정 로그 한 줄이 찍힌다.
2. 셸의 `I2C 스캔` 줄에 `0x44`가 보인다 (SHT40 하나만).
3. 셸 출력에 `WiFi 연결됨: IP=…`이 한 번 이상 나타난다.
4. 컴퓨터 브라우저로 서버의 `/nodes` 주소를 열면 방금 만든 노드가 목록에 보인다.

이 네 가지를 모두 통과시키는 것이 이번 단원의 목표입니다.

> **🧠 컴퓨팅 사고력 · 네 가지 신호의 인과 사슬**
>
> 1. LED가 꺼져 있다면 전원 단계, 셸이 비어 있다면 펌웨어 단계, I2C 스캔에 빈자리가 있다면 배선 단계, 대시보드에 카드가 없다면 네트워크 단계의 문제입니다.
> 2. 네 가지 신호는 ‘앞 단계가 통과해야 다음 신호가 켜지는’ 인과 사슬을 이룹니다.
> 3. 장애가 났을 때 ‘어디까지 켜져 있는지’를 빠르게 훑으면, 디버깅 시간이 절반으로 줄어듭니다.

> 📷 **그림 1-1** · 완성된 파일럿 노드 한 대 전체 모습
> *촬영 메모 — Pico 2 WH + Grove Shield(전원 스위치 3.3V) + SHT40 + Grove Light Sensor + 마이크로 USB 케이블이 한 컷에 들어오게. 책상 위 자연광, 위에서 약 30도 각도.*

---

## 준비물 확인

본격적인 작업에 앞서 책상 위에 다음 부품이 모두 모여 있는지 확인합니다.

| 부품 | 수량 | 비고 |
|---|---|---|
| Raspberry Pi Pico 2 WH | 1개 | 헤더가 사전 납땜된 ‘WH’ 모델 |
| Grove Shield for Pi Pico | 1개 | 피코 위에 그대로 끼우는 확장 보드 |
| SHT40 Grove 모듈 | 1개 | 온도와 습도를 함께 측정합니다 (I2C, 주소 0x44) |
| Grove Light Sensor (P) v1.1 [101020173] | 1개 | 조도를 ‘**상대 밝기 0~100%**’로 측정합니다 (아날로그 LS06-S) |
| Grove 4핀 케이블 20cm | 2개 | SHT40용 + Light Sensor용 |
| 마이크로 USB 케이블 1m | 1개 | 전원·코드 업로드 둘 다 이 한 줄로 |
| 컴퓨터 + Thonny | 1세트 | 운영체제는 Win/Mac/Linux 어느 것이라도 |
| 학교 무선망(WPA2-PSK) 정보 | SSID + 패스워드 | Pico 2 W는 EAP 인증을 지원하지 않습니다 |
| 서버 IP 주소 (**선택**) | 라즈베리 파이 5의 고정 IP | 없으면 그대로 진행 가능 — 측정·셸 로그는 정상이고 ‘전송 실패’만 찍힙니다. Unit 2 완성 후 채워도 됩니다. |

> **Tip 마이크로 USB는 ‘데이터 통신용’으로**
>
> 1. 시중 케이블 중 일부는 전원 공급만 가능하고 데이터는 흐르지 않습니다.
> 2. 데이터가 안 통하는 케이블을 쓰면 Thonny가 피코를 인식하지 못해 ‘USB를 다시 꽂아 보세요’를 무한 반복하게 됩니다.
> 3. 의심스러우면 다른 케이블로 한 번 더 시도해 봅니다.

---

## 단계 1 — 마이크로파이썬 펌웨어 설치

피코 보드 안에는 처음에는 아무 프로그램도 없습니다. 우리는 그 위에 ‘파이썬 코드를 받아 실행해 주는 인터프리터’인 마이크로파이썬(MicroPython)을 먼저 설치합니다. 이 작업은 한 번만 하면 되고, 한 번 설치된 펌웨어는 전원이 꺼져도 사라지지 않습니다.

이 단계의 핵심은 ‘**보드를 USB 메모리처럼 인식시켜, 펌웨어 파일을 그 위에 드래그&드롭한다**’는 점에 있습니다. 별도의 설치 프로그램이나 명령어가 필요 없는, 가장 단순한 펌웨어 설치 방식입니다. 다만 한 가지 조건이 있습니다. 보드를 ‘부트로더(bootloader)’ 상태로 진입시켜야 한다는 점입니다.

### 1.1 펌웨어 파일 내려받기

웹브라우저로 다음 주소에 접속해 가장 최신의 `.uf2` 파일을 내려받습니다.

```
https://micropython.org/download/RPI_PICO2_W/
```

예를 들어 `RPI_PICO2_W-20251225-v1.24.0.uf2` 같은 이름의 파일이 컴퓨터의 ‘다운로드’ 폴더에 저장됩니다. 파일 이름에 **PICO2_W**가 들어 있어야 합니다. PICO_W(1세대)와 PICO2_W(2세대)는 칩이 달라 펌웨어가 호환되지 않으니 이름을 꼭 확인합니다.

### 1.2 부트로더 모드로 진입

피코 보드의 **BOOTSEL** 글자가 인쇄된 작은 흰색 버튼을 누른 채로 마이크로 USB를 컴퓨터에 꽂습니다. 이때 버튼을 누른 상태로 USB를 끼우는 ‘동시 동작’이 중요합니다. 보드가 USB 메모리(예: `RPI-RP2` 또는 `RP2350`)로 컴퓨터에 마운트되면 그제서야 버튼에서 손을 뗍니다.

> 📷 **그림 1-2** · BOOTSEL 버튼을 누른 채로 USB를 꽂는 모습
> *촬영 메모 — 손가락이 BOOTSEL 버튼 위에 있고, 반대쪽 손이 USB 커넥터를 보드에 꽂는 순간. 보드 윗면이 잘 보이게.*

### 1.3 펌웨어 드래그&드롭

컴퓨터의 파일 탐색기에서 다운로드한 `.uf2` 파일을 방금 마운트된 `RPI-RP2` 드라이브 위로 끌어다 놓습니다. 복사가 끝나면 보드가 자동으로 재부팅되고, 드라이브가 사라집니다. 이 시점에 펌웨어 설치가 완료된 것입니다.

> **Tip 펌웨어가 정말 설치되었는지 확인**
>
> 1. 보드 본체의 작은 초록 LED가 켜져 있으면 정상 동작 신호입니다.
> 2. Thonny를 연결하면 셸에 `MicroPython v1.24.0 …` 같은 한 줄이 보입니다.
> 3. 다시 BOOTSEL 모드로 들어가지 않습니다. 보통 USB만 꽂아도 정상 부팅됩니다.

---

## 단계 2 — Thonny 설치와 보드 연결

Thonny는 마이크로파이썬 보드와 가장 친한 무료 코드 편집기입니다. 우리는 이 도구로 보드에 코드를 올리고, 셸 창에서 보드의 출력을 읽습니다.

### 2.1 Thonny 내려받기

```
https://thonny.org
```

Windows·macOS·Linux 모두에서 같은 화면을 제공합니다. 설치 후 처음 실행하면 빈 편집기가 열립니다.

### 2.2 인터프리터 설정

상단 메뉴에서 `Tools → Options → Interpreter`를 차례로 엽니다.

| 항목 | 값 |
|---|---|
| Interpreter | MicroPython (Raspberry Pi Pico) |
| Port | 자동 인식 또는 수동 선택 |

포트가 자동으로 잡히지 않을 때는 보드를 한 번 빼고 다시 꽂아 봅니다. 그래도 안 되면 다음 이름들을 후보로 둡니다.

- Windows: `COM3`, `COM5` 같은 이름
- macOS: `/dev/cu.usbmodem…` 으로 시작하는 이름
- Linux: `/dev/ttyACM0`

OK를 누른 뒤 화면 우측 아래의 빨간 정지(⏹) 버튼을 한 번 클릭합니다. 보드가 재시작되면서 셸 창에 다음과 같은 안내가 나타납니다.

```
MicroPython v1.24.0 on 2026-... ; Raspberry Pi Pico 2 W with RP2350
Type "help()" for more information.
>>>
```

`>>>` 프롬프트가 보이면 연결 성공입니다.

> 📷 **그림 1-3** · Thonny의 인터프리터 설정 화면
> *촬영 메모 — Tools > Options > Interpreter 탭이 열려 있고, MicroPython (Raspberry Pi Pico)과 포트가 선택된 모습. 우측 하단 OK 버튼이 살짝 보이게.*

### 2.3 셸에서 한 줄 시험

연결을 확인하는 가장 빠른 방법은 셸에 직접 한 줄을 입력해 보는 것입니다.

```python
>>> print("hello, pico")
hello, pico
```

`hello, pico`가 그대로 돌아오면 보드가 우리의 명령을 받고 응답하고 있다는 뜻입니다.

> **🔬 가설을 세워 봅시다 · 보드의 LED는 무엇을 알려 주는가**
>
> 1. 보드 위의 작은 초록 LED는 평소에 꺼져 있을까요, 켜져 있을까요?
> 2. `from machine import Pin; Pin("LED", Pin.OUT).on()`을 셸에 입력해 보면 어떻게 될지 예측해 봅니다.
> 3. 실제로 입력한 뒤 결과와 예측을 비교합니다. 같다면 우리는 이 보드의 LED가 ‘`Pin("LED")`에 대응한다’는 가설을 검증한 셈입니다.

---

## 단계 3 — 센서 배선

Grove Shield는 피코 위에 그대로 끼우는 확장 보드로, 4핀 Grove 커넥터를 여러 개 제공합니다. 슬롯에는 각자 다른 ‘이름’이 인쇄되어 있는데, 이번 단원에서는 두 종류의 슬롯을 씁니다 — **I2C** 슬롯에 SHT40을, **Analog(A0/A1/A2)** 슬롯에 Grove Light Sensor를 꽂습니다. 두 슬롯은 내부적으로 서로 다른 핀(`GP8/GP9` ↔ `GP26/27/28`)에 연결되어 있어 독립적으로 동작합니다.

> **Tip 📋 모듈에 인쇄된 글자가 답을 말해 줍니다**
>
> 부품 박스를 열면 가장 먼저 모듈에 인쇄된 핀 라벨을 읽습니다. 이 한 줄이 그 부품의 ‘통신 방식’을 결정합니다.
>
> 1. **`VCC / GND / SCL / SDA`** — I2C 장치. 주소(0x44 같은)로 호명되어 응답합니다. `I2C 스캔`에 나타납니다.
> 2. **`VCC / GND / NC / SIG`** — 아날로그(또는 디지털) 단일 신호 장치. `SIG` 핀 하나로 전압을 내보낼 뿐, SCL/SDA가 없어 **I2C 스캔에 절대 안 잡힙니다**. ADC로 읽어야 합니다.
> 3. 우리 키트의 Grove Light Sensor (P) v1.1에는 `NC / SIG / VCC / GND`가 인쇄되어 있습니다. 그래서 I2C가 아니라 ADC로 읽습니다(`read_light()`).
>
> **결론**: 교재의 사양을 ‘사실’로 믿지 말고 책상 위 실물의 라벨부터 읽으십시오. 이 한 줄을 30초 먼저 읽었다면 30분의 디버깅이 사라집니다.

> **Tip ⚠️ 전원 스위치는 반드시 3.3V**
>
> 1. Grove Shield 위쪽에 있는 작은 슬라이드 스위치가 `5V`와 `3.3V` 사이를 오갈 수 있습니다.
> 2. 반드시 **3.3V** 쪽으로 둡니다. 5V로 두면 Grove Light Sensor의 SIG 출력이 최대 5V까지 올라가 Pico의 ADC 입력(최대 3.3V)을 넘어 핀이 손상될 수 있습니다.
> 3. 전원을 켜기 전에 한 번 더 위치를 확인하는 습관을 만듭니다.

### 3.1 SHT40 연결 (I2C)

SHT40 Grove 모듈은 양쪽 끝이 모두 Grove 커넥터입니다.

```
[SHT40 모듈]──Grove 4핀 케이블 20cm──[Grove Shield의 I2C 슬롯]
```

Shield의 ‘I2C’라고 인쇄된 슬롯은 내부에서 Pico의 `GP8 (SDA)`·`GP9 (SCL)`로 이어져 있고, 그곳에 꽂힌 SHT40은 주소 `0x44`로 응답합니다. Grove 커넥터는 한 방향으로만 끼워지도록 설계되어 거꾸로 꽂아 손상되는 일은 없습니다.

### 3.2 Grove Light Sensor 연결 (Analog)

Light Sensor도 한 방향만 끼워지는 Grove 커넥터입니다. 다만 꽂는 슬롯이 다릅니다 — **A0** 슬롯에 꽂습니다.

```
[Grove Light Sensor (P) v1.1]──Grove 4핀 케이블 20cm──[Grove Shield의 A0 슬롯]
```

Shield의 `A0`는 Pico의 `GP26 (= ADC0)`에 연결되어 있습니다. 센서의 SIG 핀이 빛의 밝기에 따라 0V~3.3V 사이의 전압을 내보내고, Pico가 그 전압을 0~65535의 정수로 읽어 우리는 그것을 다시 0~100%로 환산합니다.

### 3.3 두 센서가 서로 다른 통신 방식을 쓰는 이유

같은 보드 위의 두 센서가 한쪽은 ‘I2C’, 한쪽은 ‘아날로그’입니다. 왜 통일하지 않았을까요? 센서의 원리가 다르기 때문입니다. SHT40은 안에 작은 디지털 회로가 있어 ‘**측정 명령을 받으면 16비트 숫자로 답해 준다**’는 약속을 합니다. 그래서 I2C 같은 디지털 통신이 적합합니다. 반면 Grove Light Sensor는 LS06-S라는 포토트랜지스터 한 알이 빛의 양에 따라 전류만 흘리는 가장 단순한 부품입니다. 디지털로 말할 거리가 없으니, 그저 ‘**빛이 강할수록 SIG 핀에 더 높은 전압이 걸린다**’가 전부입니다. 그것을 읽는 가장 단순한 방법이 Pico의 ADC입니다.

> 📷 **그림 1-4** · SHT40(I2C 슬롯) + Grove Light Sensor(A0 슬롯)
> *촬영 메모 — Grove Shield 위 ‘I2C’ 인쇄 슬롯과 ‘A0’ 인쇄 슬롯에 케이블이 각각 꽂혀 있고, 반대쪽 두 센서가 책상에 펼쳐진 모습. 좌측 상단에 ‘3.3V’ 위치의 전원 스위치가 잘 보이게.*

---

## 단계 4 — 펌웨어 파일 올리기

우리는 펌웨어 소스를 미리 세 개의 파일로 정리해 두었습니다. 각 파일이 맡은 일이 분명히 나뉘어 있어, 한 곳이 잘못되면 어디를 봐야 할지 곧바로 알 수 있습니다.

| 파일 | 역할 |
|---|---|
| `sensors.py` | SHT40(I2C)과 Grove Light Sensor(ADC)를 ‘읽어 오는’ 함수 모음 |
| `main.py` | WiFi 연결, 측정, 서버 전송, 와치독 등 메인 루프 |
| `secrets.py` | 노드별로 다르게 채워 넣는 비밀값(WiFi, NODE_ID, 서버 URL) |

### 4.1 Thonny에서 파일 패널 열기

상단 메뉴 `View → Files`를 선택합니다. 화면 왼쪽에 두 개의 패널이 열립니다.

- 위: **This computer** — 본인 컴퓨터의 폴더
- 아래: **MicroPython device** — 보드 내부

### 4.2 세 파일 업로드

위 패널에서 다음 폴더로 이동합니다.

```
~/greatsong-project/climate-action-365/prototype/firmware/
```

`main.py`·`sensors.py`·`secrets.py` 세 파일을 모두 선택한 뒤 우클릭으로 **Upload to /** 를 누르면 아래 패널(보드)로 그대로 올라갑니다.

> 📷 **그림 1-5** · Thonny의 파일 패널에서 세 파일이 보드로 업로드된 모습
> *촬영 메모 — 좌측 위/아래 두 패널이 모두 보이고, 아래 패널(보드)에 세 파일이 나란히 보이는 화면.*

### 4.3 `secrets.py` 노드별 값 채우기

아래 패널의 `secrets.py`를 더블 클릭해 엽니다. 다음 네 줄을 우리 학교 환경에 맞게 수정합니다.

#### 코드 1-1

```python
WIFI_SSID = "DanggokIoT"                                       # 학교 IoT 전용 SSID
WIFI_PASSWORD = "여기에_패스워드"                              # 학교가 정한 패스워드
NODE_ID = "PILOT"                                              # 파일럿 단계에서는 PILOT
SERVER_URL = "http://192.168.0.10:8000/reading"                # 라즈베리 파이 5의 고정 IP
INTERVAL_SEC = 30                                              # 30초마다 측정·전송
```

저장(Ctrl+S 또는 Cmd+S)을 누르면 보드 안의 파일이 즉시 갱신됩니다.

> **Tip 서버 IP가 아직 없다면 — 그대로 진행해도 됩니다**
>
> 1. Unit 2(라즈베리 파이 5 서버) 완성 전이라도 Unit 1은 끝까지 진행할 수 있습니다.
> 2. `SERVER_URL`을 그대로 두면 펌웨어는 측정과 셸 출력은 정상으로 돌고, 30초마다 한 번씩 ‘전송 실패’ 로그만 찍힙니다. 측정값은 코드 1-3의 `buffer.jsonl`에 그대로 쌓이므로 데이터 손실은 없습니다.
> 3. 더 빠르게 서버 응답까지 보고 싶다면, 본인 컴퓨터에서 한 줄로 임시 서버를 띄울 수 있습니다.
>
>    ```bash
>    cd ~/greatsong-project/climate-action-365/prototype/server
>    python3 -m venv venv && source venv/bin/activate
>    pip install -r requirements.txt
>    uvicorn server:app --host 0.0.0.0 --port 8000
>    ```
>
>    이 경우 `SERVER_URL`은 노트북의 IP(예: `http://192.168.0.123:8000/reading`)로 박아 둡니다.
> 4. Unit 2 완성 후 진짜 라즈베리 파이 5의 IP로 바꾸고 보드를 재부팅하면, 그동안 buffer에 쌓여 있던 측정값까지 한 번에 서버로 올라갑니다(자동 flush).

### 4.4 `sensors.py` — 센서 드라이버 코드 읽어 보기

업로드만 하고 넘어가도 동작은 하지만, 이 파일이 ‘어떻게 SHT40에게는 디지털로 말을 걸고 Light Sensor에게는 아날로그로 듣는지’를 한 번 읽어 두면 다음에 다른 센서를 붙일 때 손이 빨라집니다. 그리고 이 코드 안에는 ‘**부팅 직후 첫 측정에서 가끔 NAK가 난다**’는 미세한 현실에 대응하는 두 줄 — 측정 대기 20ms와 3회 재시도 — 가 숨어 있습니다.

#### 코드 1-2

```python
"""
SHT40 (온습도, I2C) + Grove Light Sensor (P) v1.1 (아날로그) MicroPython 드라이버.

Grove Shield for Pi Pico 매핑 (당곡고 부품 기준, 실측 검증):
- I2C0: SDA=GP8, SCL=GP9  → SHT40 (주소 0x44)
- Analog A0: GP26          → Grove Light Sensor (SIG)

⚠️ Shield 전원 스위치는 반드시 3.3V로 둡니다.
"""

from machine import I2C, Pin, ADC
import time

I2C_ID = 0
SDA_PIN = 8
SCL_PIN = 9
I2C_FREQ = 100_000

SHT40_ADDR = 0x44
SHT40_CMD_HIGHPRECISION = b"\xFD"
SHT40_WAIT_MS = 20      # 데이터시트 8.2ms + 안정 여유. 10ms는 가끔 부족.
SHT40_RETRIES = 3       # 부팅 직후 NAK 대응

_i2c = I2C(I2C_ID, sda=Pin(SDA_PIN), scl=Pin(SCL_PIN), freq=I2C_FREQ)

LIGHT_ADC_PIN = 26
_light_adc = ADC(Pin(LIGHT_ADC_PIN))

# LM358 OP-amp(non rail-to-rail) 보정 — 자세한 설명은 트러블슈팅 사다리 🪜 8.
LIGHT_RAW_MIN = 500
LIGHT_RAW_MAX = 42000


def scan():
    """I2C 주소 목록 — hex 문자열로 반환. SHT40만 있으면 ['0x44']."""
    return [hex(a) for a in _i2c.scan()]


def read_sht40():
    """SHT40에서 (온도°C, 습도%RH). 최대 3회 재시도."""
    last_err = None
    for _ in range(SHT40_RETRIES):
        try:
            _i2c.writeto(SHT40_ADDR, SHT40_CMD_HIGHPRECISION)
            time.sleep_ms(SHT40_WAIT_MS)
            data = _i2c.readfrom(SHT40_ADDR, 6)
            t_raw = (data[0] << 8) | data[1]
            rh_raw = (data[3] << 8) | data[4]
            temperature = -45.0 + 175.0 * (t_raw / 65535.0)
            humidity = -6.0 + 125.0 * (rh_raw / 65535.0)
            if humidity < 0:
                humidity = 0.0
            elif humidity > 100:
                humidity = 100.0
            return temperature, humidity
        except OSError as e:
            last_err = e
            time.sleep_ms(50)
    raise last_err


def read_light_raw():
    return _light_adc.read_u16()


def read_light():
    raw = _light_adc.read_u16()
    span = LIGHT_RAW_MAX - LIGHT_RAW_MIN
    pct = (raw - LIGHT_RAW_MIN) / span * 100.0
    if pct < 0:
        pct = 0.0
    elif pct > 100:
        pct = 100.0
    return pct


def read_all():
    """한 번 호출로 모든 센서 측정. (온도°C, 습도%RH, 조도%) 반환."""
    t, rh = read_sht40()
    light = read_light()
    return t, rh, light
```

##### 핵심 한 줄 풀이

`read_sht40()` 안의 환산식 `temperature = -45.0 + 175.0 * (t_raw / 65535.0)`을 한 번 들여다봅니다. 센서가 보내 주는 두 바이트(0~65535)는 그 자체로는 의미가 없습니다. SHT40의 데이터시트가 정한 ‘**0이면 -45°C, 65535면 130°C로 직선 환산하라**’는 약속이 이 한 줄에 들어 있습니다.

같은 사고방식이 `read_light()`에서도 다시 등장합니다. ADC가 0~65535의 정수로 알려 준 값을 65535로 나누고 100을 곱해 ‘**가장 어두울 때 0%, 가장 밝을 때 100%**’로 옮깁니다. 다만 LS06-S의 출력은 정확한 lux가 아니라 ‘빛의 상대 세기’이므로, 절대값(예: 학교보건법 ‘책상면 300 lux 이상’)을 직접 매핑할 수는 없습니다. 분석팀이 학기 초에 ‘우리 학교 1교시 형광등 ON 평균 = N%’를 측정해 자기만의 임계값을 정하는 것이 정공법입니다.

### 4.5 `main.py` — 메인 루프 코드 읽어 보기

`main.py`는 매 30초마다 ‘**측정 → 서버 전송 → 실패하면 flash에 저장 → 다음 회차에 함께 재전송**’이라는 사이클을 무한 반복합니다. WiFi가 끊겼을 때를 대비한 재연결 로직과, 무한 루프 방지용 와치독(Watchdog) 타이머가 함께 들어 있습니다.

#### 코드 1-3

```python
"""
교실 환경 모니터링 노드 — Phase 1 (온습도 + 조도).

동작:
1. WiFi 연결 (실패 시 지수 백오프 재시도, 5분 넘으면 reset)
2. SHT40로 온습도, Grove Light Sensor로 조도(%) 측정
3. JSON으로 서버에 HTTP POST
4. INTERVAL_SEC 만큼 대기 후 반복

장애 대응:
- 와치독 타이머 8초로 무한루프 방지
- 네트워크 끊김 시 측정값을 flash에 버퍼링(최대 200건)했다가 재전송
"""

import json
import time
import network
import urequests
from machine import WDT, reset

import secrets
import sensors


BUFFER_FILE = "buffer.jsonl"
BUFFER_MAX = 200

wdt = WDT(timeout=8000)


def log(msg):
    print("[{}] {}".format(time.ticks_ms(), msg))


def connect_wifi():
    """WiFi 연결. 지수 백오프로 재시도. 5분 넘으면 reset."""
    wlan = network.WLAN(network.STA_IF)
    wlan.active(True)

    if wlan.isconnected():
        return wlan

    delay = 2
    deadline = time.time() + 300  # 5분
    while not wlan.isconnected():
        wdt.feed()
        log("WiFi 연결 시도: {}".format(secrets.WIFI_SSID))
        try:
            wlan.connect(secrets.WIFI_SSID, secrets.WIFI_PASSWORD)
        except OSError as e:
            log("connect 오류: {}".format(e))

        for _ in range(10):
            wdt.feed()
            if wlan.isconnected():
                break
            time.sleep(1)

        if wlan.isconnected():
            break

        if time.time() > deadline:
            log("5분 내 연결 실패 → reset")
            reset()

        log("{}초 후 재시도".format(delay))
        for _ in range(delay):
            wdt.feed()
            time.sleep(1)
        delay = min(delay * 2, 60)

    log("WiFi 연결됨: IP={}".format(wlan.ifconfig()[0]))
    return wlan


def buffer_append(payload):
    """전송 실패한 측정값을 flash에 추가 저장."""
    try:
        try:
            with open(BUFFER_FILE, "r") as f:
                lines = f.readlines()
        except OSError:
            lines = []
        lines.append(json.dumps(payload) + "\n")
        if len(lines) > BUFFER_MAX:
            lines = lines[-BUFFER_MAX:]
        with open(BUFFER_FILE, "w") as f:
            f.writelines(lines)
    except OSError as e:
        log("buffer 쓰기 실패: {}".format(e))


def buffer_flush():
    """버퍼링된 측정값을 한 번씩 재전송 시도. 성공하면 파일 비움."""
    try:
        with open(BUFFER_FILE, "r") as f:
            lines = f.readlines()
    except OSError:
        return

    if not lines:
        return

    log("버퍼 {}건 재전송 시도".format(len(lines)))
    survivors = []
    for line in lines:
        wdt.feed()
        try:
            payload = json.loads(line)
            if not send_once(payload, retry=False):
                survivors.append(line)
        except ValueError:
            pass

    try:
        with open(BUFFER_FILE, "w") as f:
            f.writelines(survivors)
    except OSError:
        pass


def send_once(payload, retry=True):
    """payload를 서버에 한 번 전송. 성공하면 True."""
    try:
        r = urequests.post(
            secrets.SERVER_URL,
            json=payload,
            headers={"Content-Type": "application/json"},
            timeout=5,
        )
        ok = 200 <= r.status_code < 300
        r.close()
        return ok
    except Exception as e:
        log("전송 실패: {}".format(e))
        return False


def measure():
    """센서 1회 측정. 실패하면 None 필드로 반환."""
    payload = {
        "node_id": secrets.NODE_ID,
        "ts": time.time(),
        "temperature": None,
        "humidity": None,
        "light": None,
    }
    try:
        t, rh = sensors.read_sht40()
        payload["temperature"] = round(t, 2)
        payload["humidity"] = round(rh, 2)
    except Exception as e:
        log("SHT40 오류: {}".format(e))

    try:
        payload["light"] = round(sensors.read_light(), 1)
    except Exception as e:
        log("Light 센서 오류: {}".format(e))

    return payload


def main():
    log("부팅: node_id={}".format(secrets.NODE_ID))
    log("I2C 스캔: {} (0x44 = SHT40 하나만 보이면 정상)".format(
        [hex(a) for a in sensors.scan()]))

    connect_wifi()

    while True:
        wdt.feed()

        payload = measure()
        log("측정: T={} RH={} light={}%".format(
            payload["temperature"], payload["humidity"], payload["light"]))

        if send_once(payload):
            buffer_flush()
        else:
            buffer_append(payload)
            wlan = network.WLAN(network.STA_IF)
            if not wlan.isconnected():
                connect_wifi()

        elapsed = 0
        while elapsed < secrets.INTERVAL_SEC:
            wdt.feed()
            time.sleep(1)
            elapsed += 1


main()
```

##### 핵심 한 줄 풀이

이 코드의 가장 영리한 부분은 **`buffer.jsonl` 파일을 통한 오프라인 버퍼링**입니다. 서버나 WiFi가 잠시 죽어도, 측정값은 일단 Pico의 flash에 한 줄씩 차곡차곡 쌓입니다. 다음 번 전송이 성공하면 그 동안 쌓인 줄을 함께 보내고, 파일을 비웁니다. 학교 망이 점검 등으로 30분 끊기더라도 그 시간 동안의 데이터가 모두 살아남는 이유가 여기에 있습니다. 최대 200건(약 100분치)까지 보관하므로 그보다 긴 단절은 가장 오래된 데이터부터 잘려 나갑니다.

또 하나 짚어 둘 부분은 **`WDT(timeout=8000)`** 한 줄입니다. ‘8초 안에 코드가 한 번이라도 `wdt.feed()`를 호출하지 않으면 보드를 강제로 재시작한다’는 약속을 마이크로파이썬에게 거는 명령입니다. 코드가 어떤 이유로든 무한 루프에 빠지면, 8초 뒤 보드가 스스로 살아나도록 합니다. 사람이 16개 교실을 매일 들여다보지 않아도 시스템이 스스로를 추스르도록 만드는 가장 작은 장치입니다.

> **🧠 컴퓨팅 사고력 · 버퍼링과 와치독, 두 가지 ‘자기 치유’**
>
> 1. 오프라인 버퍼링(`buffer.jsonl`)은 ‘외부 환경(서버·WiFi)이 잠시 죽어도 데이터는 안 잃는다’는 약속입니다.
> 2. 와치독 타이머(`WDT`)는 ‘내부 코드가 죽어도 보드는 다시 깨어난다’는 약속입니다.
> 3. 두 가지가 함께 있을 때 우리는 ‘잊고 살 수 있는 노드 한 대’를 가지게 됩니다. 16대로 늘릴 때 사람의 손이 16배가 되지 않는 비결이 이 두 줄입니다.

> **🤖 AI에게 물어보기 · 같은 파일을 16번 다른 값으로 만드는 방법**
>
> 1. 16개 교실 각각의 `secrets.py`를 손으로 16번 고치는 일은 지루합니다. 우리는 곧 Unit 3에서 ‘하나의 템플릿에서 16개의 파일을 자동으로 만드는 방법’을 보게 됩니다.
> 2. 시간이 남으면 챗 LLM에 ‘NODE_ID만 다른 16개의 파일을 한 번에 만들어 주는 셸 스크립트를 작성해 주세요’라고 부탁해 보세요. 어떤 코드가 돌아오는지 비교해 봅니다.
> 3. 받은 스크립트가 안전한지 검증할 줄 아는 사람이, AI를 잘 쓰는 사람입니다.

---

## 단계 5 — 첫 부팅과 검증

우측 하단의 빨간 정지(⏹) 버튼을 누르면 보드가 한 번 더 재시작되며 `main.py`가 자동으로 실행됩니다.

##### 실행 결과

```
[12345] 부팅: node_id=PILOT
[12450] I2C 스캔: ['0x44'] (0x44 = SHT40 하나만 보이면 정상)
[12500] WiFi 연결 시도: DanggokIoT
[13200] WiFi 연결됨: IP=192.168.0.123
[13800] 측정: T=24.31 RH=45.2 light=42.7%
```

이 다섯 줄이 5~15초 안에 차례로 나와야 합니다. 줄마다 어떤 의미가 담겨 있는지 한 번 짚어 봅니다.

1. **부팅** — `secrets.py`의 `NODE_ID`가 정확히 읽혔다는 신호.
2. **I2C 스캔** — `0x44`(SHT40)가 잡혔다는 신호. 빠지면 SHT40 배선 단계로 돌아갑니다. Grove Light Sensor는 I2C가 아니라 ADC라 이 줄에 안 나타나는 것이 정상입니다.
3. **WiFi 연결 시도 / 연결됨** — 학교 무선망에 정상적으로 붙었다는 신호. IP가 보이지 않으면 SSID·패스워드를 다시 확인합니다.
4. **측정** — 실제 센서값이 30초마다 한 줄씩 새로 찍히는 신호.

### 측정값이 합리적인 범위인가

실제 환경에서 의미 있는 값이 나오는지를 빠르게 가늠해 봅니다. 첫 번째 측정값이 다음 범위 안에 들어와야 ‘정상’이라고 판단합니다.

| 항목 | 정상 범위 (교실 실내) |
|---|---|
| 온도 T | 15 ~ 35 °C |
| 습도 RH | 20 ~ 80 %RH |
| 조도 light | 약 20 ~ 80 % (형광등이 켜진 평일 낮 기준 · 직사광 시 90% 이상) |

극단값이 보이면(예: T가 -45.0 또는 130.0) 센서 read 자체가 실패한 것이고, 보통 케이블이 흔들렸기 때문입니다.

> 📷 **그림 1-6** · Thonny 셸에 흐르는 첫 측정 로그
> *촬영 메모 — Thonny 하단 셸 창을 클로즈업. ‘I2C 스캔’과 ‘측정’ 두 줄이 잘 보이게.*

> **📊 데이터 잠깐 들여다보기 · 한 점의 의미**
>
> 1. 30초마다 한 줄씩 쌓이는 측정값은 하루에 약 2,880점이 됩니다. 16노드라면 하루 약 46,000점입니다.
> 2. 이 데이터의 한 점은 ‘우리 교실의 지금 이 순간’입니다. 한 점만 보면 평범한 숫자이지만, 1주일치를 모아 그리면 ‘월요일 1교시에 환기가 어떻게 되는가’라는 패턴이 됩니다.
> 3. Unit 4에서 우리는 이 데이터로 학교보건법 기준값을 넘는 시간을 세어 보게 됩니다.

---

## 단계 6 — 서버 수신 확인

이제 우리가 보낸 측정값이 라즈베리 파이 서버에 실제로 도착했는지를 두 군데에서 확인합니다.

### 6.1 JSON 응답으로 확인

컴퓨터 브라우저로 다음 주소를 엽니다.

```
http://192.168.0.10:8000/nodes
```

JSON 응답에 `PILOT` 노드가 한 줄 보이고, `last_seen`이 1분 이내라면 성공입니다.

#### 코드 1-4

```json
[
  {
    "node_id": "PILOT",
    "last_seen": "2026-05-21T13:45:23+00:00",
    "reading_count": 8,
    "latest": {
      "temperature": 24.31,
      "humidity": 45.2,
      "light": 42.7
    }
  }
]
```

### 6.2 대시보드로 확인

같은 컴퓨터에서 다음 주소를 엽니다.

```
http://192.168.0.10:8501
```

‘전체 교실’ 탭의 카드 한 장에 `PILOT` 노드가 표시되고, 세 줄의 측정값이 합리적인 색상(🟩 적정)으로 보이면 모든 단계가 통과된 것입니다.

> 📷 **그림 1-7** · 대시보드 ‘전체 교실’ 탭에 표시된 PILOT 카드
> *촬영 메모 — Streamlit 대시보드의 카드 한 장이 잘 보이게 클로즈업. 온도·습도·조도 세 줄과 ‘마지막 수신’ 시각이 보이게.*

---

## 🔍 통합 검증 체크리스트

마지막으로 다섯 가지를 한 번에 확인해 봅니다.

- [ ] 보드 본체 초록 LED가 켜져 있다
- [ ] Thonny 셸에 30초마다 ‘측정’ 한 줄이 새로 찍힌다
- [ ] I2C 스캔에 `0x44`(SHT40)가 보인다 — Grove Light Sensor는 ADC라 여기 안 나옴
- [ ] WiFi 연결됨 메시지가 한 번 이상 나타났다
- [ ] 대시보드의 ‘전체 교실’ 탭에 노드 카드가 보인다

다섯 가지를 모두 통과했다면, 한 대로 16대의 동작 원리를 모두 확인한 것입니다. 축하하는 의미에서 스스로에게 박수를 쳐 주기 바랍니다 👏👏👏.

## 🛠 수정·확장 미션

1. `INTERVAL_SEC`을 30에서 10으로 바꿔 봅니다. 측정 로그가 정말 10초마다 찍히나요?
2. Grove Light Sensor를 A0 슬롯에서 A1(GP27) 슬롯으로 옮겨 꽂으면 어떻게 될지 예측하고, 실제로 바꿔 본 뒤 `sensors.py`의 `LIGHT_ADC_PIN`도 27로 바꿔 봅니다.
3. USB를 뽑고 1분 동안 기다린 뒤 다시 꽂아 봅니다. 같은 NODE_ID 카드의 ‘마지막 수신’ 시각이 어떻게 변하는지 관찰합니다.

## 🌟 리터러시 모먼트

> **🧠 컴퓨팅 사고력 · 변하지 않는 부분과 변하는 부분**
>
> 1. `main.py`와 `sensors.py`는 16개 노드에서 한 글자도 다르지 않습니다. `secrets.py`만 노드마다 다릅니다.
> 2. ‘무엇을 같게, 무엇을 다르게’ 두는지에 대한 결정이 좋은 시스템 설계의 절반입니다.
> 3. Unit 3에서 이 ‘다르게 두는 부분만 자동으로 16번 만드는’ 패턴을 다시 보게 됩니다.

> **🤖 AI에게 물어보기 · 셸 메시지에 답을 묻기**
>
> 1. 만약 어떤 단계에서 에러가 나면, Thonny 셸의 빨간 줄을 통째로 챗 LLM에 붙여 ‘이 에러를 어떻게 해석합니까?’라고 물어 봅니다.
> 2. AI의 답이 처음부터 끝까지 맞는 일은 흔치 않습니다. ‘제일 마지막 줄’부터 읽는 습관, ‘파일 이름과 줄 번호’를 같이 보내는 습관이 답의 품질을 크게 올려 줍니다.

---

## 🛠 트러블슈팅 사다리 — 먼저 다녀온 사람이 남긴 7가지 레슨런

이 단원의 가장 큰 함정은 ‘교재의 사양을 사실로 믿는 것’입니다. 코드는 거의 죄가 없습니다. 하드웨어가 교재와 다를 때 무엇을 어떤 순서로 의심해야 하는지 — 다음 7개의 박스가 그 사다리입니다.

> **🪜 1. 교재 사양을 사실로 믿지 마라**
>
> 우리 키트의 실물은 교재 초기 설계와 네 가지가 달랐습니다 — I2C 핀(GP4/GP5 → GP8/GP9), 조도 센서(BH1750 → Grove Light Sensor), I2C 스캔 기대값(`['0x44','0x23']` → `['0x44']`), 버스 공유(둘 다 I2C → SHT40만 I2C). 본문은 이미 실측 결과로 갱신했지만, 다음 부품을 받을 때도 같은 함정이 반복됩니다. **부품 박스를 열면 모듈 라벨부터 읽으십시오.**

> **🪜 2. `[]`와 `[68]`은 완전히 다른 신호다**
>
> - `I2C 스캔: []` → 버스에 응답하는 장치가 **0개**. 주소(0x44)를 의심하기 전에 **핀·전원·배선** 같은 앞 단계가 문제입니다.
> - `I2C 스캔: [68]` → `68`은 `0x44`의 십진수. 즉 SHT40은 **이미 잡혔다**. 옛 `scan()`이 hex 변환을 안 했을 뿐(본문 코드는 hex 문자열 반환). 통신은 되는 것이니 안심하십시오.
> - `[Errno 5] EIO` → 빈 버스에서 read를 시도했을 때 나는 후속 증상. 스캔이 채워지면 같이 사라집니다. **EIO 자체를 쫓지 말고 스캔부터 살리십시오.**

> **🪜 3. 핀을 모를 때는 추측 말고 전수 스캔**
>
> Shield의 슬롯 라벨이 실제 핀과 일치하지 않을 수 있습니다. Thonny 셸에 다음을 그대로 붙여 한 번에 모든 후보를 훑으십시오.

#### 코드 1-5 — I2C 핀 후보 전수 스캔

```python
from machine import Pin, I2C
for sda, scl, idx in [(4,5,0),(6,7,1),(0,1,0),(8,9,0),(2,3,1)]:
    try:
        bus = I2C(idx, sda=Pin(sda), scl=Pin(scl), freq=100000)
        print("SDA=GP%d SCL=GP%d ->" % (sda,scl), [hex(a) for a in bus.scan()])
    except Exception as e:
        print("SDA=GP%d SCL=GP%d ->" % (sda,scl), "err", e)
```

한 줄이라도 `['0x44']`가 떠 있는 행이 정답입니다. 그 SDA/SCL을 `sensors.py`의 `SDA_PIN`·`SCL_PIN`에 박아 두면 끝입니다. 한 시간 추측을 다섯 줄로 끝내는 도구입니다.

> **🪜 4. 라벨이 답을 말해 준다 — NC/SIG vs SCL/SDA**
>
> 단계 3에서 이미 살펴봤지만 한 번 더 — `VCC/GND/NC/SIG`가 인쇄된 모듈은 영원히 I2C 스캔에 안 잡힙니다. 그 부품은 ADC로 읽어야 합니다. `read_light()`처럼.

> **🪜 5. 에러 문구를 믿지 말고 셸에서 단독 호출하라**
>
> ‘배선 확인’이라는 에러 문구를 곧이곧대로 따르다 한 시간을 잃을 수 있습니다. 같은 동작을 셸에서 단독으로 한 번 시켜 보십시오.

#### 코드 1-6 — 셸 단독 호출 진단

```python
import sensors
print(sensors.scan())          # ['0x44'] 이어야 함
print(sensors.read_sht40())    # 예: (22.76, 53.2)
print(sensors.read_light())    # 예: 42.7
print(sensors.read_all())      # 예: (22.76, 53.2, 42.7)
```

거기서 정상값이 나오면 코드 호출 맥락(타이밍·순서)의 문제입니다. 본문 `sensors.py`는 측정 대기 20ms와 3회 재시도로 그 타이밍 문제를 이미 흡수합니다.

> **🪜 6. MCU 루프는 주변장치에 시간을 양보해라**
>
> `srv.settimeout(0.0)`처럼 accept를 완전 논블로킹으로 두면 main 루프가 5ms마다 미친 듯이 회전해 CYW43(WiFi) 칩이 일할 틈을 못 얻습니다. 그러면 `[CYW43] do_ioctl: timeout`이 반복되고 접속이 끊깁니다. **30ms 타임아웃**을 주는 것만으로 WiFi 스택이 숨을 쉽니다 — `main_pilot.py`의 `srv.settimeout(0.03)` 한 줄이 그것입니다(아래 ‘옵션 모드’ 참조).

> **🪜 7. 다 고쳤는데 안 되면, 파일이 진짜 올라갔는지 검증해라**
>
> `'module' object has no attribute 'read_all'` 같은 에러는 보통 ‘`main.py`만 새로 올리고 `sensors.py`는 옛 버전을 그대로 둔’ 상황입니다. Thonny 셸에서 한 번 확인하십시오.

#### 코드 1-7 — 보드의 파일이 실제로 갱신되었는지 검증

```python
import sensors
print(hasattr(sensors, 'read_all'))   # True 여야 함
print(sensors.scan())                 # ['0x44'] (문자열 리스트) 이어야 함
```

기대한 결과가 안 나오면 코드가 틀린 게 아니라 **옛 파일이 보드에 남아 있는** 것입니다. Thonny 파일 패널에서 새로 업로드한 뒤 보드를 한 번 더 재시작하십시오.

> **🪜 8. 조도가 64.5%에서 더 안 올라가는 이유 — 회로의 동적 범위 한계**
>
> 햇볕 아래 형광등을 향해 비춰도 `light` 값이 약 **64.5%에서 멈추는** 현상은 정상입니다. Grove Light Sensor (P) v1.1의 출력 회로는 **LM358 OP-amp 비반전 증폭기**인데, LM358은 ‘non rail-to-rail’이라 단일 공급 전압에서 출력이 VCC까지 못 올라가고 약 **(VCC − 1.2V)** 에서 saturate 합니다.
>
> - Shield **3.3V** 공급 → SIG 출력 최대 ≈ **2.13V** ≈ ADC raw **42,300** ≈ **64.5%** (정확히 관측값과 일치)
> - 즉 이 센서의 ‘실제 100%’는 raw 65,535가 아니라 raw ≈ 42,000 근처입니다.

#### 코드 1-8 — 우리 학교 환경에 맞춰 보정값 측정

본문 `sensors.py`는 이미 `LIGHT_RAW_MIN = 500`, `LIGHT_RAW_MAX = 42000`을 디폴트로 두어 0~100%를 다시 펴 줍니다. 우리 학교 환경의 실제 saturate 한계가 다르면 셸에서 한 번 측정해 그 값을 박으면 됩니다.

```python
import sensors, time
# 1) 책상 밑·서랍 안 등 가장 어두운 자리에 두고 10초간 평균
samples = [sensors.read_light_raw() for _ in range(50)]
print("어둠 raw 평균:", sum(samples)//50)   # → 이 값을 LIGHT_RAW_MIN 후보

# 2) 형광등 ON 책상면 또는 창가 직사광 자리에서 10초간 평균
input("가장 밝은 자리로 옮긴 뒤 Enter")
samples = [sensors.read_light_raw() for _ in range(50)]
print("최대 raw 평균:", sum(samples)//50)   # → 이 값을 LIGHT_RAW_MAX 후보
```

두 측정값을 `sensors.py` 상단의 상수에 박은 뒤 파일을 다시 업로드하면, ‘**우리 학교 가장 어두운 자리 = 0%, 가장 밝은 자리 = 100%**’가 됩니다. 분석팀이 학기 초에 한 번 보정해 두면 1년 내내 의미 있는 % 값이 됩니다.

> **🔬 가설을 세워 봅시다 · 왜 0~100%로 펴서 쓰는가**
>
> 1. raw 값 그대로 두면 ‘65535가 최대’라고 가정한 수식이 64.5%에서 멈춰 답답합니다.
> 2. 회로 한계를 인정하고 ‘우리 환경 안에서의 0~100%’로 다시 정규화하면, 사람의 직관(‘반쯤 밝다 = 50%’)과 데이터가 맞아 떨어집니다.
> 3. 정확한 lux가 아니라 ‘상대 밝기’임을 받아들이는 것이 이 센서를 잘 쓰는 첫 단추입니다.

---

## 옵션 모드 — `main_pilot.py`로 폰 브라우저에서 보기

Unit 2의 라즈베리 파이 서버가 아직 없을 때도 ‘파일럿 한 대’의 가치를 끝까지 보고 싶다면, Pico 자체를 임시 HTTP 서버로 만들 수 있습니다. 폰을 같은 WiFi에 연결한 뒤 Pico의 IP를 주소창에 치면, 5초마다 자동 갱신되는 온도·습도·조도 페이지가 그대로 보입니다.

### 사용법

1. Thonny 파일 패널에서 `prototype/firmware/main_pilot.py`를 보드에 올립니다.
2. 셸에서 한 줄:
   ```python
   import main_pilot
   ```
3. 셸에 다음 한 줄이 보이면 완료:
   ```
   [12345] HTTP 준비: http://192.168.0.123/
   ```
4. 폰 브라우저 주소창에 그 URL을 입력합니다.

### 코드의 핵심 한 줄

`srv.settimeout(0.03)` — 단 한 줄이 ‘들어오는 접속을 놓치지 않으면서도 30ms마다 측정 루프를 돌게’ 만듭니다. 이 한 줄을 0(완전 논블로킹)으로 바꾸면 WiFi 칩이 일할 틈을 못 얻어 페이지가 열리지 않습니다. 마이크로컨트롤러에서 ‘**적절한 sleep/timeout은 성능 저하가 아니라 협업**’이라는 원칙이 이 한 줄에 담겨 있습니다.

> 📷 **그림 1-8** · 폰 브라우저에서 본 Pico 파일럿 페이지
> *촬영 메모 — 폰을 가로로 든 브라우저 화면. 주소창에 `http://192.168.x.x/`, 본문에 ‘🌱 교실 환경 · NODE PILOT’ 헤더와 온도·습도·조도 세 카드가 보이게.*

> **🧠 컴퓨팅 사고력 · ‘서버가 없으면 서버가 된다’**
>
> 1. Unit 1의 본 모드(`main.py`)는 ‘피코 = 클라이언트’입니다. 측정값을 라즈베리 파이에 보냅니다.
> 2. 옵션 모드(`main_pilot.py`)는 ‘피코 = 서버’입니다. 폰의 브라우저가 클라이언트로 와서 측정값을 받아 갑니다.
> 3. 같은 부품이 누구를 마주하느냐에 따라 ‘서버’도 되고 ‘클라이언트’도 됩니다. 시스템 설계자의 시점이 그렇게 만듭니다.

---

## 옵션 모드 v2 — `main_pilot_v2.py`로 라이브 그래프 + 메모

`main_pilot.py`가 ‘현재 카드 3장’만 보여 줬다면, `main_pilot_v2.py`는 한 단계 진화해 **라이브 시계열 그래프 + 메모 기록 + CSV 다운로드**까지 한 페이지에 담습니다. ‘몇 시부터 더웠고 몇 시부터 추웠는지’가 한눈에 들어오고, ‘점심시간 환기 시작’ 같은 관찰 메모를 폰에서 입력해 두면 나중에 CSV로 받아 그대로 참고 자료로 쓸 수 있습니다.

### 동작 요약

| 항목 | 값 |
|---|---|
| 측정 주기 | **3초** |
| 메모리 ring | **최근 30분 (600건)** |
| flash CSV | `data.csv` 누적, 200KB 초과 시 자동 절반 rotate |
| 메모 | `notes.csv` 누적, 페이지에서 form 입력 |
| 페이지 auto-refresh | **3초** |
| 시각 동기화 | NTP → KST(UTC+9) 자동 |

### 페이지 구성

1. **헤더**: 🟢 라이브 펄스 도트 + NODE ID + 현재 시각 + 측정 카운터 + uptime
2. **카드 3장**: 온도 / 습도 / 조도 — 1분 전 대비 트렌드 화살표 `↑ ↓ →`
3. **SVG 그래프 3장**: 30분 X축, 5분 간격 격자, 마지막 점에 현재값 라벨
4. **메모 입력 폼**: textarea + ‘저장’ 버튼 → POST `/note` → 시각과 함께 `notes.csv`에 추가
5. **최근 메모 5건**
6. **다운로드 링크**: `/data.csv`, `/notes.csv`, `/clear` (초기화)

### 사용법

1. `prototype/firmware/main_pilot_v2.py`를 Thonny 파일 패널에서 보드에 업로드 (`sensors.py`·`secrets.py`도 함께)
2. 셸에서 한 줄:
   ```python
   import main_pilot_v2
   ```
3. 셸의 `HTTP 준비: http://192.168.x.x/` 한 줄 → 폰 브라우저에서 그 URL
4. 3초마다 그래프가 갱신되고, 메모를 입력하면 즉시 목록에 추가
5. 측정 끝나면 `/data.csv` 클릭 → 폰 다운로드 폴더에 CSV 저장 → 엑셀에서 그대로 열림

> **📊 데이터 잠깐 들여다보기 · 메모가 데이터를 ‘설명’으로 바꾼다**
>
> 1. 측정값만 있으면 ‘14:23에 22.4°C였다’ 입니다.
> 2. 그 옆에 ‘14:22 창문 열기 시작’ 메모가 한 줄 있으면, 비로소 ‘**14:22에 환기를 시작한 결과 22.4°C로 내려갔다**’가 됩니다.
> 3. 시계열 데이터 분석에서 ‘이벤트 라벨’이 의미를 만듭니다. 이번 단원의 메모 기능이 그 가장 단순한 예입니다.

> 📷 **그림 1-9** · `main_pilot_v2.py`의 라이브 페이지
> *촬영 메모 — 폰 화면 캡처. 라이브 도트 + 카드 3장(트렌드 화살표 보이게) + 30분 시계열 그래프 3장 + 입력된 메모 2~3건이 보이게.*

전체 코드는 [`prototype/firmware/main_pilot_v2.py`](https://github.com/greatsong/climate-action-365/blob/main/prototype/firmware/main_pilot_v2.py)에 있습니다. 약 380줄.

## 마무리

한 대를 세웠으니, 이제 같은 작업을 자동화할 차례입니다. Unit 2에서는 16대 모두를 받아 줄 라즈베리 파이 5 서버를 한 번에 셋업하고, Unit 3에서 16대의 펌웨어를 한 줄 명령으로 한꺼번에 배포해 보겠습니다. 이번 단원에서 손에 익은 ‘LED → 셸 → 서버 → 대시보드’의 인과 사슬은 16배가 되어도 그대로입니다.

## 연습문제

**1.** I2C 스캔에 아무것도 안 보이는 상황을 가정해 봅니다. SHT40 배선에서 가장 먼저 의심해야 할 한 가지는 무엇인가요?

**2.** `secrets.py`의 `NODE_ID`를 `"1-1"`로, 또 다른 보드는 `"1-2"`로 채웠습니다. 두 보드가 같은 WiFi에 동시에 붙어 있을 때, 서버의 `/nodes` 응답에는 카드가 몇 장 보일까요?

**3.** `INTERVAL_SEC = 30`을 `INTERVAL_SEC = 1`로 바꾸면 어떤 일이 일어날지 두 가지 이상 예측합니다. *(힌트: 보드의 부담, 서버의 부담, 디스크 사용량)*

**4.** *(도전)* 셸 출력의 `WiFi 연결됨: IP=…`이라는 한 줄은 사실 매우 많은 일이 끝났음을 의미합니다. 그 ‘많은 일’ 중에 세 가지를 적어 봅니다.

> 정답은 부록 D에 수록되어 있습니다.
